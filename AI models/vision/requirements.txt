# HireMind — Python dependencies
# Pin strategy: minimum version known to work; upper-bounded where breakage is
# likely on the next major release.  Tested on Python 3.10 / 3.11.
#
# Install:
#   pip install -r requirements.txt
#
# Optional engines (uncomment what you need):
#   Vosk  → pip install vosk
#           then set VOSK_MODEL_PATH env var (see README)
#   Azure → pip install azure-cognitiveservices-speech

# ── Web server ────────────────────────────────────────────────────────────────
fastapi>=0.103.0,<1.0.0
uvicorn[standard]>=0.23.0,<1.0.0
starlette>=0.27.0,<1.0.0
websockets>=11.0,<13.0

# ── Vision ────────────────────────────────────────────────────────────────────
opencv-python-headless>=4.8.0,<5.0.0
mediapipe>=0.10.0,<0.11.0
numpy>=1.24.0,<2.0.0

# ── AI / Emotion (DeepFace pulls TensorFlow; pin to avoid MediaPipe conflicts)
deepface>=0.0.79,<0.1.0
tensorflow>=2.13.0,<2.16.0       # must match mediapipe's bundled runtime

# ── Speech — primary (Vosk, local, offline) ───────────────────────────────────
vosk>=0.3.44,<0.4.0

# ── Speech — fallback (Whisper, local, heavier) ───────────────────────────────
openai-whisper>=20230918

# ── Report generation ─────────────────────────────────────────────────────────
openpyxl>=3.1.0,<4.0.0

# ── Utilities ─────────────────────────────────────────────────────────────────
python-multipart>=0.0.6          # needed by FastAPI for form data
